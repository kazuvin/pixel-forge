{
  "name": "@pixel-forge/web",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node -e \"import('@astrojs/check')\" && astro check",
    "test": "node --test tests/*.test.mjs",
    "build": "astro build",
    "deploy:dry-run": "WRANGLER_WRITE_LOGS=false wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^7.1.3",
    "typescript": "^6.0.3",
    "wrangler": "^4.113.0"
  }
}
